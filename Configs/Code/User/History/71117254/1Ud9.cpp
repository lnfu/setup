#include <bits/stdc++.h>

using namespace std;

int main()
{
    int N;
    cin >> N;
    bool status = false; // 0
    for (int i = 0; i < N; ++i)
    {
        int X, Y;
        cin >> X >> Y;
        if (X == 0)
        {
        }
        else // X == 1
        {
            ;
        }
    }
    return 0;
}